export enum SessionState {
  established = 'established',
  notLoggedIn = 'notLoggedIn',
  establishing = 'establishing',
  tokenExpired = 'tokenExpired',
  terminated = 'terminated',
}

export enum EUserRoles {
  'channel-moderator' = 'channel-moderator',
}
